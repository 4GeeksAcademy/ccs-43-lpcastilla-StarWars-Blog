import React , {useState} from "react";

export const Favorites = () => {
	const [favorites, setFavorites] = useState ([]);
    return (
        <button className="btn btn-primary dropdown-toggle">Favorites
        </button>
    ):
};
        
			
